apiVersion: v1
kind: Secret
metadata:
  name: api-secret
  namespace: dev
type: Opaque
stringData:  # Using stringData - no base64 encoding needed
  API_TOKEN: "super-secret-token-123"
  DB_PASSWORD: "postgres123"
