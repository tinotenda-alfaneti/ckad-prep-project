apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
  namespace: dev
data:
  MESSAGE: "Hello from CKAD ConfigMap!"
  LOG_LEVEL: "info"
  MAX_WORKERS: "4"
  # Can also store file content
  app.properties: |
    server.port=8080
    server.host=0.0.0.0
    cache.enabled=true
